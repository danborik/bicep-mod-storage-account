/* ==========================================
  
  This is a Bicep module template use by the main Bicep file as a submodule.
  The submodule name is the name of the folder where the Bicep file is located.
  The submodule file name is always main.bicep.

  Keep the following Bicep code structure:
  - Parameters
  - Variables
  - Resources / Modules
  - Outputs

   ========================================== */
